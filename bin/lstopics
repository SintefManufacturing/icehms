#!/usr/bin/env python
import icehms
if __name__ == "__main__":
    mgr = icehms.IceManager()
    try:
        mgr.init()
        topics = mgr.get_all_topics()
        print("\nTopics are: \n")
        for name, prx in topics.items():
            print(name)
    finally:
        mgr.shutdown()

